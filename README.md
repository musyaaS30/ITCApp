1. backend sama frontend di `npm install` dulu
2. env disesuaiin 😁
3. di backend `npx prisma migrate dev`, seed `npx prisma db seed` buat adminnya dan dummy usernya
4. `npm run start:dev` - backend, `npm run dev` - frontend
